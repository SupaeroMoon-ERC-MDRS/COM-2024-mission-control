abstract class ConfigurationManager{
  static bool reconfig(){
    // TODO put remote path in gs config
    // TODO put netcode path in gs config
    // TODO put dbc path in gs config
    return true;
  }
}